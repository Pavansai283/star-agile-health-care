apiVersion: v1
kind: Service
metadata:
  name: healthcare-service
spec:
  selector:
     app: healthcare
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30010
